node_modules
.next
.next-build
.git
.env
.env.local
tests
scripts/demo-data.sql
*.log
.DS_Store
# the separated frontend is its own image; the app has no use for its source
# and including it made `next build` try to compile it without its deps
web
